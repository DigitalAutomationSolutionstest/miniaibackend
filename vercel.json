{
  "redirects": [
    { "source": "/", "destination": "/api/debug" }
  ],
  "buildCommand": "cd backend && npm install && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "cd backend && npm install",
  "framework": "nextjs",
  "outputDirectory": "backend/.next"
}
